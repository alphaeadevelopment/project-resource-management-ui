// import { createSelector } from 'reselect';

export const getAllRoles = state => state.staticdata.roles.all;
export const getAllLocations = state => state.staticdata.locations.all;
export const getAllResources = state => state.staticdata.resources.all;

